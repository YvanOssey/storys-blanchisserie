Validation admin connectée : avec une session OAuth administrateur active, `/admin` affiche le tableau de bord Story’s, la navigation Commandes/Clients/Paiements et les données de gestion. Les procédures serveur admin exigent désormais `adminProcedure` ; les visiteurs reçoivent `UNAUTHORIZED` et les utilisateurs connectés sans rôle admin `FORBIDDEN`.

Vérification visuelle : `/admin` reste accessible et correctement rendu pour la session administrateur OAuth active, avec navigation de gestion et tableau de bord Story’s visibles.
Constat du contrôle déconnecté : après confirmation utilisateur, l’aperçu Sandbox affiche encore la session admin active sur `/admin` et `/admin/orders`. Les deux routes sont protégées côté serveur par `adminProcedure`, mais une preuve navigateur sans session n’est pas encore obtenue dans cette session persistante.
Validation responsive desktop : le bouton « Se déconnecter » est maintenant visible directement dans le footer de la barre latérale sur `/admin` et `/admin/orders`, sans ouvrir le menu du profil. Les deux écrans conservent leur navigation et leurs données pour une session admin valide.
Test de déconnexion directe : le bouton « Se déconnecter » est bien visible et ciblable sur `/admin/orders`. Le navigateur Sandbox conserve toutefois l’affichage de la session après le clic automatisé ; la déconnexion devra être confirmée depuis l’interface utilisateur ou vérifiée par la protection serveur déjà testée.
Après attente et consultation de la console, aucune erreur JavaScript n’est remontée. La session Sandbox reste toutefois affichée ; le contrôle déconnecté sera donc confirmé par l’utilisateur dans son navigateur ou par les tests backend, qui distinguent bien `UNAUTHORIZED` et `FORBIDDEN`.
Validation desktop après amélioration : le bouton sombre « Se déconnecter » est immédiatement visible dans l’en-tête de `/admin` et `/admin/orders`, en plus de l’action disponible dans le footer de la barre latérale.
Validation mobile après amélioration : sur `/admin` comme sur `/admin/orders`, le bouton « Se déconnecter » reste fixé dans la première zone visible, avec une taille lisible et une cible tactile confortable. La navigation mobile et le registre restent utilisables.
Validation déconnectée réelle : après la déconnexion effectuée par l’utilisateur, `/admin` et `/admin/orders` affichent uniquement la page Story’s « Votre atelier, en ordre. » avec le bouton « Ouvrir mon espace » ; aucune donnée de tableau de bord ou commande n’est visible.
Validation navigateur : depuis l’espace admin connecté, le bouton d’en-tête « Se déconnecter » a ramené vers `/admin` et affiche le formulaire « Connexion admin » avec e-mail administrateur, mot de passe, bouton « Se connecter à l’administration » et option « Connexion propriétaire Manus » pour la première configuration.
Validation utilisateur : la connexion admin dédiée par e-mail/mot de passe fonctionne après initialisation depuis l’accès propriétaire et ajout à la whitelist.
Validation navigateur après le correctif non bloquant : le clic sur « Se déconnecter » a affiché immédiatement `/admin` avec le formulaire « Connexion admin », sans rester sur le tableau de bord.
