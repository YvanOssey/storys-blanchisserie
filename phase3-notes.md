Correction de l’intégration des notifications admin : l’insertion de la table `adminNotifications` avait laissé une ligne résiduelle dans `drizzle/schema.ts`, supprimée immédiatement. Le contrôle TypeScript est revenu à zéro erreur après correction.
Diagnostic test notification : le test existant du planning utilisait `dbMock.upsertOrderAssignment`, mais ce mock n’était pas déclaré. La correction consiste à l’ajouter au double DB avant de relancer la suite.
Incident connexion publiée : `/admin` sur blanchapp-b3fltebp.manus.space affiche correctement le formulaire dédié. Les options de cookie utilisent HTTPS, path `/` et `SameSite=None` avec `secure` selon la requête. Les logs consultés ne montrent pas encore de tentative `adminAuth.login`; le diagnostic nécessite donc l’adresse admin utilisée et le message affiché après clic.
Validation visuelle de la refonte notifications : sur desktop et mobile, le bloc d’alertes a disparu du contenu principal du dashboard. La cloche avec badge non lu est visible dans l’en-tête près de la déconnexion et reste utilisable en largeur mobile.
Incident cloche invisible : sur le domaine publié, `/admin` affiche l’écran de connexion admin lorsqu’aucune session admin n’est active. La cloche n’est donc pas visible avant authentification ; elle est prévue dans l’en-tête du dashboard après connexion, près de « Se déconnecter ».
Nouvelle demande notifications : `CustomerSpace.tsx` affiche actuellement les notifications dans un bloc principal et limite l’affichage aux quatre premières. Les helpers DB disposent de `listCustomerNotifications(customerId)` mais pas encore d’actions client pour compter, marquer comme lu ou supprimer ; l’isolation se fera par `customerId` issu de la session.
Contrôle visuel après propagation : sur `/admin`, la cloche est visible dans l’en-tête immédiatement à gauche de « Se déconnecter ». Sur `/mon-espace`, l’écran reste en ouverture sans session client active, ce qui est attendu après la purge des comptes clients.
